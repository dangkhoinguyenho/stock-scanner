"""Roadmap Phase 8 — the market-wide scanner.

Applies the learned event/market-condition relationships to the live
universe and ranks current opportunities. Every output must show its
evidence (comparable events, base rate, avg/median return, confidence) —
never a bare verdict (ARCHITECTURE.md rule 1).

Not started yet — empty on purpose.
"""
